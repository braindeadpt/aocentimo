# Pedidos — sessão 3B «O que pagas»

Sem pedidos. A sessão usou os componentes congelados tal como estão
(`Pagina`, `PaginaDetalhe`, `Cartao`, `CampoCentimos`, `Isometrico`,
`Haltere`, `LineChart`, `Regua`, `Segmentado`, `NumHero`, `Odometer`,
`EstadoVazio`, `Source`, `Icone`) e não tocou em `globals.css` nem em
`messages/pt.json` (a copy das rotas segue o padrão existente —
strings na página, `m.*` só para o chrome partilhado).

---

## Decisão da Sessão 4 (2026-09-23)

- Confirmado: sem pedidos pendentes. `LineChart` está em uso real em
  `/credito`, `/dados`, `/inflacao`, `/precos`, `/estilo`, `Leitura` e
  `EmptyState` — a menção a «old LineChart» no brief não corresponde a
  nenhum ficheiro órfão.
